<Prefab>
  <Window>
    <ButtonWidget SuggestedWidth="251" SuggestedHeight="64" VerticalAlignment="Center" HorizontalAlignment="Left"
                  IsEnabled="@IsEnabled" IsVisible="@IsButton" DoNotPassEventsToChildren="true" Brush="MCM.Cancel.Button" Command.Click="OnValueClick">
      <Children>
        <TextWidget WidthSizePolicy="StretchToParent" HeightSizePolicy="StretchToParent" VerticalAlignment="Center" HorizontalAlignment="Center"
                    IsEnabled="@IsEnabled" IsVisible="@IsButtonVisible" Text="@ButtonContent" Brush="MCM.Button.Text" />
      </Children>
    </ButtonWidget>
  </Window>
</Prefab>